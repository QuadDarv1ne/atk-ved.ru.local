# ============================================
# Redis Cache Configuration for WordPress
# ============================================
# Скопируйте этот файл в docker-compose.yml или .env
# и раскомментируйте нужные настройки

# ============================================
# REDIS (рекомендуется)
# ============================================

# Хост Redis (для Docker)
WP_REDIS_HOST=redis

# Порт Redis
WP_REDIS_PORT=6379

# Таймаут подключения (секунды)
WP_REDIS_TIMEOUT=1

# Пароль Redis (если требуется)
# WP_REDIS_PASSWORD=your_secure_password

# Номер базы данных (0-16)
WP_REDIS_DATABASE=0

# Соль для ключей кэша (уникальная для каждого сайта)
WP_CACHE_KEY_SALT=atk_ved_ru_local_

# ============================================
# MEMCACHED (альтернатива Redis)
# ============================================

# Хост Memcached
# WP_MEMCACHED_HOST=memcached

# Порт Memcached
# WP_MEMCACHED_PORT=11211

# ============================================
# WORDPRESS CACHE SETTINGS
# ============================================

# Включить объектный кэш
WP_CACHE=true

# Путь к файлу объектного кэша
# WP_CACHE_KEY_SALT=your_unique_salt_here

# ============================================
# DOCKER COMPOSE EXAMPLE (redis service)
# ============================================
#
# services:
#   redis:
#     image: redis:7-alpine
#     container_name: atk_ved_redis
#     restart: unless-stopped
#     ports:
#       - "6379:6379"
#     volumes:
#       - redis_data:/data
#     command: redis-server --appendonly yes
#     healthcheck:
#       test: ["CMD", "redis-cli", "ping"]
#       interval: 10s
#       timeout: 5s
#       retries: 5
#
# volumes:
#   redis_data:
