/**
 * Counters - Hero и Statistics
 * @package ATK_VED
 */
(function($){'use strict';const stats=[{value:500,suffix:'+',label:'Довольных клиентов'},{value:5,suffix:'+',label:'Лет на рынке'},{value:1000,suffix:'+',label:'Контейнеров доставлено'},{value:15,suffix:'',label:'Городов доставки'}];let countersAnimated=false;function initHeroCounters(){const $heroSection=$('.hero-section');if(!$heroSection.length)return;const statsHTML='<div class="hero-stats">'+stats.map((stat,index)=>'<div class="stat-item" data-index="'+index+'"><div class="stat-number" data-target="'+stat.value+'"><span class="stat-value">0</span><span class="stat-suffix">'+stat.suffix+'</span></div><div class="stat-label">'+stat.label+'</div></div>').join('')+'</div>';$heroSection.find('.hero-content').after(statsHTML);initScrollAnimation()}function initScrollAnimation(){$(window).on('scroll',function(){if(countersAnimated)return;const $heroStats=$('.hero-stats');if(!$heroStats.length)return;const statsTop=$heroStats.offset().top;const windowBottom=$(window).scrollTop()+$(window).height();if(windowBottom>statsTop+100){countersAnimated=true;animateCounters()}});$(window).trigger('scroll')}function animateCounters(){$('.stat-item').each(function(index){const $item=$(this);const $number=$item.find('.stat-number');const $value=$item.find('.stat-value');const target=parseInt($number.data('target'));setTimeout(function(){$item.addClass('animated');$number.addClass('counting');animateCounter($value,target);setTimeout(function(){$number.removeClass('counting')},2000)},index*150)})}function animateCounter($element,target){const duration=2000;const steps=60;const stepDuration=duration/steps;const increment=target/steps;let current=0;const timer=setInterval(function(){current+=increment;if(current>=target){current=target;clearInterval(timer)}const formatted=Math.floor(current).toString().replace(/\B(?=(\d{3})+(?!\d))/g,' ');$element.text(formatted)},stepDuration)}function createStatisticsSection(){const statsHTML='<section class="statistics-section"><div class="container"><div class="statistics-grid">'+stats.map((stat,index)=>'<div class="stat-card"><div class="stat-icon">'+['👥','📅','📦','🌍'][index]+'</div><div class="stat-number" data-target="'+stat.value+'">0</div><div class="stat-suffix">'+stat.suffix+'</div><div class="stat-label">'+stat.label+'</div></div>').join('')+'</div></div></section>';$('.advantages-section').after(statsHTML)}function initStatisticsAnimation(){let animated=false;$(window).on('scroll',function(){if(animated)return;const $section=$('.statistics-section');if(!$section.length)return;const sectionTop=$section.offset().top;const windowBottom=$(window).scrollTop()+$(window).height();if(windowBottom>sectionTop+100){animated=true;$('.stat-card').each(function(index){const $card=$(this);const $number=$card.find('.stat-number');const target=parseInt($number.data('target'));setTimeout(function(){$card.addClass('animate');animateCounter($number,target)},index*200)})}})}$(document).ready(function(){initHeroCounters();createStatisticsSection();initStatisticsAnimation();$(window).trigger('scroll')})})(jQuery);
