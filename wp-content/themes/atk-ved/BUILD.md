# 🚀 Сборка и оптимизация темы АТК ВЭД

## Требования

- **Node.js** >= 18.0.0
- **npm** >= 9.0.0
- **PHP** >= 8.1
- **WordPress** >= 6.0

---

## 📦 Установка зависимостей

```bash
cd wp-content/themes/atk-ved
npm install
```

---

## 🔧 Команды сборки

### Разработка

```bash
# Запуск сервера разработки с hot-reload
npm run dev

# Сборка в режиме watch (без hot-reload)
npm run watch
```

### Продакшен

```bash
# Полная сборка с минификацией
npm run build

# Предпросмотр продакшен сборки
npm run preview
```

---

## 🖼️ Оптимизация изображений

### Генерация WebP версий

```bash
# Создать .webp файлы для всех изображений
npm run generate:webp
```

**Что делает:**
- Находит все JPG/PNG изображения
- Создаёт WebP версии с качеством 80%
- Показывает процент экономии для каждого файла

### Оптимизация существующих изображений

```bash
# Сжать все изображения
npm run optimize:images
```

**Что делает:**
- Уменьшает размер JPG/PNG без потери качества
- Изменяет размер больших изображений (макс. 1920x1080)
- Сохраняет оригинальные пропорции

### Критический CSS

```bash
# Генерация критического CSS для быстрой загрузки
npm run critical
```

**Что делает:**
- Извлекает стили для первого экрана
- Создаёт inline CSS для мгновенной отрисовки
- Уменьшает Time to First Paint

---

## 📊 Структура файлов после сборки

```
atk-ved/
├── css/
│   ├── modern-design.css       # Исходный CSS
│   ├── modern-design.min.css   # Минифицированная версия
│   ├── critical.css            # Критический CSS (inline)
│   └── *.webp                  # WebP версии изображений
├── js/
│   ├── main.js                 # Исходный JS
│   ├── main.min.js             # Минифицированная версия
│   └── *.min.js                # Остальные скрипты
├── dist/                       # Продакшен сборка
│   ├── css/
│   ├── js/
│   └── manifest.json           # Карта ресурсов
└── package.json
```

---

## ⚡ Настройки производительности

### В Customizer (Внешний вид → Настроить → Производительность)

| Опция | Описание | Рекомендация |
|-------|----------|--------------|
| **Включить кэширование** | HTTP заголовки кэширования | ✅ Включено |
| **Включить минификацию** | Минификация CSS/JS | ✅ Включено |
| **Удалять неиспользуемые стили** | Deregister ненужных CSS | ✅ Включено |
| **Аналитика производительности** | Логи в консоли | 🔶 По желанию |

### В `.env` (для Docker)

```env
# Оптимизация
WP_CACHE=true
WP_DEBUG=false
WP_DEBUG_DISPLAY=false
SCRIPT_DEBUG=false
COMPRESS_CSS=true
COMPRESS_SCRIPTS=true
CONCATENATE_SCRIPTS=true
ENFORCE_GZIP=true
```

---

## 🎯 Best Practices

### 1. Перед деплоем

```bash
# 1. Оптимизировать изображения
npm run optimize:images
npm run generate:webp

# 2. Собрать минифицированные файлы
npm run build

# 3. Сгенерировать критический CSS
npm run critical

# 4. Проверить качество
npm run lint:css
npm run lint:js
```

### 2. Lazy Loading

Изображения автоматически получают `loading="lazy"`:

```html
<!--Above-the-fold (ленивая загрузка отключена)-->
<img class="hero-image" loading="eager" fetchpriority="high">

<!--Ниже линии сгиба (ленивая загрузка включена)-->
<img loading="lazy" decoding="async">
```

### 3. WebP с фоллбэком

```html
<picture>
  <source srcset="image.webp" type="image/webp">
  <img src="image.jpg" alt="..." loading="lazy">
</picture>
```

---

## 📈 Ожидаемые улучшения

| Метрика | До | После | Улучшение |
|---------|-----|-------|-----------|
| **PageSpeed Score** | 60-70 | 90-95 | +30-40% |
| **First Contentful Paint** | 2.5s | 0.8s | -68% |
| **Largest Contentful Paint** | 4.0s | 1.5s | -62% |
| **Total Blocking Time** | 800ms | 200ms | -75% |
| **Размер страницы** | 2.5MB | 0.8MB | -68% |

---

## 🔍 Проверка производительности

### Инструменты

```bash
# Google PageSpeed Insights
https://pagespeed.web.dev/

# Lighthouse (в Chrome DevTools)
F12 → Lighthouse → Generate report

# WebPageTest
https://www.webpagetest.org/

# GTmetrix
https://gtmetrix.com/
```

### Локальное тестирование

```bash
# Запустить локальный сервер
npm run preview

# Открыть в браузере
http://localhost:4173
```

---

## 🛠️ Troubleshooting

### Ошибка: `npm install` завершается с ошибкой

```bash
# Очистить кэш npm
npm cache clean --force

# Удалить node_modules и package-lock.json
rm -rf node_modules package-lock.json

# Установить заново
npm install
```

### Ошибка: Vite не находит файлы

Проверьте пути в `vite.config.js`:

```js
const themeDir = resolve(__dirname); // Должен указывать на папку темы
```

### Ошибка: WebP не генерируется

Проверьте наличие GD или Imagick:

```php
// В wp-admin → Инструменты → Здоровье сайта
// Проверка: "Библиотека изображений"
```

---

## 📚 Дополнительные ресурсы

- [Vite Documentation](https://vitejs.dev/)
- [Sharp Documentation](https://sharp.pixelplumbing.com/)
- [WordPress Performance](https://developer.wordpress.org/advanced-administration/performance/)
- [WebP Documentation](https://developers.google.com/speed/webp)

---

**Версия:** 3.1.0  
**Последнее обновление:** Февраль 2026
