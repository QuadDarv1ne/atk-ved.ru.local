# ATK VED - Environment Configuration
# =============================================================================
# Скопируйте этот файл в .env и настройте под свои нужды
# Команда: cp .env.example .env (Linux/Mac) или copy .env.example .env (Windows)
# =============================================================================

# =============================================================================
# ОКРУЖЕНИЕ
# =============================================================================
# Возможные значения: local, development, production
WP_ENV=local

# =============================================================================
# БАЗА ДАННЫХ
# =============================================================================
# Для Docker: используйте mysql как хост
# Для OpenServer: localhost
# Для продакшена: укажите ваш хост БД

WP_DB_NAME=atk_ved
WP_DB_USER=root
WP_DB_PASSWORD=
WP_DB_HOST=localhost

# Docker настройки (раскомментировать для Docker)
# MYSQL_DATABASE=wordpress
# MYSQL_USER=wordpress
# MYSQL_PASSWORD=wordpress
# WORDPRESS_DB_HOST=mysql

# Префикс таблиц (по умолчанию wp_)
WP_TABLE_PREFIX=wp_

# =============================================================================
# РЕЖИМ ОТЛАДКИ
# =============================================================================
# Включайте только для разработки!
# На продакшене должно быть false

# Включить режим отладки (true/false)
WP_DEBUG=true

# Логировать ошибки в файл wp-content/debug.log (true/false)
WP_DEBUG_LOG=true

# Показывать ошибки на экране (true/false)
# ВАЖНО: На продакшене должно быть false!
WP_DEBUG_DISPLAY=true

# Отладка скриптов (разработка с минифицированными файлами)
SCRIPT_DEBUG=true

# =============================================================================
# ПРОИЗВОДИТЕЛЬНОСТЬ
# =============================================================================

# Лимит памяти WordPress (по умолчанию 256M)
WP_MEMORY_LIMIT=256M

# Максимальный лимит памяти для админки
WP_MAX_MEMORY_LIMIT=512M

# Включить кэширование (для продакшена)
# WP_CACHE=true

# Количество ревизий постов (0 = отключить, false = неограничено)
WP_POST_REVISIONS=5

# Интервал автосохранения в секундах
AUTOSAVE_INTERVAL=300

# Очистка корзины (в днях, 0 = не очищать)
EMPTY_TRASH_DAYS=7

# =============================================================================
# БЕЗОПАСНОСТЬ
# =============================================================================

# Запретить редактирование файлов через админку
# На продакшене: true
DISALLOW_FILE_EDIT=false

# Запретить установку плагинов/тем
# На продакшене: true
DISALLOW_FILE_MODS=false

# Принудительное использование SSL в админке
# На продакшене: true
FORCE_SSL_ADMIN=false

# =============================================================================
# URL САЙТА
# =============================================================================
# Для Docker: http://localhost:8080
# Для OpenServer: http://atk-ved.ru.local
# Для продакшена: https://ваш-домен.ru

WP_HOME=http://atk-ved.ru.local
WP_SITEURL=http://atk-ved.ru.local

# =============================================================================
# DOCKER НАСТРОЙКИ
# =============================================================================

# Порт для доступа к сайту (Docker)
HTTP_PORT=8080

# Порт для phpMyAdmin (Docker)
PMA_PORT=8081

# Порт для Mailhog (Docker)
MAILHOG_PORT=8025

# Root пароль MySQL (Docker)
MYSQL_ROOT_PASSWORD=rootpassword

# =============================================================================
# КЭШИРОВАНИЕ (Redis)
# =============================================================================
# Раскомментировать при использовании Redis

# WP_REDIS_HOST=127.0.0.1
# WP_REDIS_PORT=6379
# WP_REDIS_TIMEOUT=1
# WP_REDIS_READ_TIMEOUT=1

# =============================================================================
# SMTP НАСТРОЙКИ (для отправки email)
# =============================================================================
# Раскомментировать и настроить для продакшена

# SMTP_HOST=smtp.yandex.ru
# SMTP_PORT=465
# SMTP_USER=your-email@yandex.ru
# SMTP_PASSWORD=your-password
# SMTP_FROM=your-email@yandex.ru
# SMTP_FROM_NAME=АТК ВЭД

# =============================================================================
# КРОН
# =============================================================================

# Отключить WP-Cron (если используете системный)
# DISABLE_WP_CRON=true

# Альтернативный WP-Cron
# ALTERNATE_WP_CRON=false

# =============================================================================
# ПРИМЕЧАНИЯ
# =============================================================================
#
# 1. Не добавляйте .env в git! Он уже в .gitignore
# 2. Для продакшена создайте отдельный .env с безопасными настройками
# 3. Значения true/false автоматически конвертируются в boolean
# 4. Пустые значения конвертируются в false
# 5. Все константы можно переопределить в wp-config.php
# 6. Для генерации ключей безопасности используйте:
#    https://api.wordpress.org/secret-key/1.1/salt/
#
# =============================================================================
# КЛЮЧИ БЕЗОПАСНОСТИ (только для продакшена!)
# =============================================================================
# Сгенерируйте уникальные ключи для продакшена на:
# https://api.wordpress.org/secret-key/1.1/salt/

# AUTH_KEY=generate-unique-key-here
# SECURE_AUTH_KEY=generate-unique-key-here
# LOGGED_IN_KEY=generate-unique-key-here
# NONCE_KEY=generate-unique-key-here
# AUTH_SALT=generate-unique-salt-here
# SECURE_AUTH_SALT=generate-unique-salt-here
# LOGGED_IN_SALT=generate-unique-salt-here
# NONCE_SALT=generate-unique-salt-here
