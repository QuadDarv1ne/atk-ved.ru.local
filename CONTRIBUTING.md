# Руководство для участников проекта АТК ВЭД

Благодарим за интерес к проекту АТК ВЭД! Это руководство поможет вам начать вносить свой вклад.

## 📋 Содержание

- [Кодекс поведения](#кодекс-поведения)
- [Как внести вклад](#как-внести-вклад)
- [Стандарты кода](#стандарты-кода)
- [Тестирование](#тестирование)
- [Pull Request процесс](#pull-request-процесс)

---

## 🤝 Кодекс поведения

### Наши обязательства

- Создавать дружелюбную и безопасную среду
- Уважать различные мнения и опыт
- Принимать конструктивную критику
- Фокусироваться на том, что лучше для сообщества

### Недопустимое поведение

- Использование сексуализированного языка
- Оскорбления и личные нападки
- Публикация личной информации других людей
- Другое неэтичное поведение

---

## 🚀 Как внести вклад

### 1. Сообщить об ошибке

Перед созданием issue:
- Проверьте существующие issue
- Используйте ясный заголовок
- Опишите шаги для воспроизведения
- Приложите скриншоты если возможно

### 2. Предложить функцию

- Опишите проблему которую решает функция
- Приведите примеры использования
- Обсудите с maintainers перед началом работы

### 3. Внести код

```bash
# Форкните репозиторий
git fork https://github.com/yourusername/atk-ved

# Клонируйте форк
git clone https://github.com/yourusername/atk-ved.git

# Создайте ветку
git checkout -b feature/your-feature-name

# Внесите изменения
# ...

# Закоммитьте
git commit -m "feat: добавить новую функцию"

# Отправьте
git push origin feature/your-feature-name
```

---

## 📝 Стандарты кода

### PHP

Следуем [WordPress Coding Standards](https://developer.wordpress.org/coding-standards/wordpress-coding-standards/).

```php
<?php
/**
 * Пример функции
 *
 * @since 2.4.0
 * @param string $param Описание параметра
 * @return mixed Возвращаемое значение
 */
function atk_ved_example_function( $param ) {
    // Проверка безопасности
    if ( empty( $param ) ) {
        return new WP_Error( 'empty_param', __( 'Параметр пуст', 'atk-ved' ) );
    }
    
    // Основная логика
    $result = some_operation( $param );
    
    /**
     * Фильтр для изменения результата
     *
     * @since 2.4.0
     * @param mixed  $result Результат
     * @param string $param  Входной параметр
     */
    return apply_filters( 'atk_ved_example_result', $result, $param );
}
```

**Правила:**
- Используйте префикс `atk_ved_` для всех функций
- Документируйте функции PHPDoc
- Используйте `strict_types=1`
- Экранируйте вывод (`esc_html`, `esc_url`, etc.)
- Проверяйте nonce для форм

### JavaScript

```javascript
/**
 * Пример функции
 * 
 * @since 2.4.0
 * @param {Object} options Настройки
 * @return {void}
 */
(function($) {
    'use strict';
    
    function atkVedExampleFunction(options) {
        var settings = $.extend({
            debug: false
        }, options);
        
        if (settings.debug) {
            console.log('Debug mode');
        }
    }
    
    // Экспорт для глобального доступа
    window.atkVedExampleFunction = atkVedExampleFunction;
    
})(jQuery);
```

**Правила:**
- Используйте `'use strict'`
- Документируйте функции JSDoc
- Используйте префикс `atkVed` для глобальных функций
- Избегайте глобальных переменных

### CSS

```css
/**
 * Пример компонента
 *
 * @since 2.4.0
 */

.component-name {
    /* Переменные */
    --component-color: var(--color-primary);
    
    /* Свойства */
    display: flex;
    align-items: center;
    gap: var(--spacing-4);
    
    /* Состояния */
    &:hover {
        --component-color: var(--color-primary-dark);
    }
    
    /* Модификаторы */
    &--large {
        gap: var(--spacing-6);
    }
}
```

**Правила:**
- Используйте CSS переменные
- Следуйте методологии BEM
- Документируйте компоненты
- Используйте `var(--spacing-*)` для отступов

---

## 🧪 Тестирование

### PHPUnit тесты

```bash
# Запуск всех тестов
vendor/bin/phpunit

# Запуск конкретного теста
vendor/bin/phpunit --filter test_atk_ved_get_field

# Запуск с coverage
vendor/bin/phpunit --coverage-html ./coverage
```

### QUnit тесты

```bash
# Запуск тестов
npm test

# Запуск в watch режиме
npm run test:watch
```

### Ручное тестирование

1. Проверьте функциональность в браузере
2. Протестируйте на мобильных устройствах
3. Проверьте доступность (keyboard navigation)
4. Проверьте производительность (Lighthouse)

---

## 📦 Pull Request процесс

### 1. Создание PR

- Создайте ветку от `develop`
- Дайте ясное название
- Опишите изменения
- Приложите скриншоты если применимо

### 2. Шаблон PR

```markdown
## Описание
Краткое описание изменений

## Тип изменений
- [ ] 🐛 Исправление ошибки
- [ ] ✨ Новая функция
- [ ] 📝 Документация
- [ ] ⚡ Улучшение производительности

## Чек-лист
- [ ] Код следует стандартам
- [ ] Добавлены тесты
- [ ] Обновлена документация
- [ ] Протестировано вручную

## Скриншоты
(если применимо)

## Related issues
Fixes #123
```

### 3. Code Review

- Минимум 1 approval от maintainer
- Исправьте все замечания
- Пройдите CI проверки

### 4. Merge

- Squash and merge для feature веток
- Rebase and merge для небольших изменений

---

## 🏷️ Convention Commits

Используем [Conventional Commits](https://www.conventionalcommits.org/):

```
feat: добавить новую функцию
fix: исправить ошибку
docs: обновить документацию
style: исправить стиль кода
refactor: рефакторинг кода
test: добавить тесты
chore: обновить зависимости
```

**Примеры:**
```bash
git commit -m "feat: добавить калькулятор доставки"
git commit -m "fix: исправить ошибку в модальном окне"
git commit -m "docs: обновить README"
```

---

## 📚 Ресурсы

- [WordPress Developer Handbook](https://developer.wordpress.org/)
- [ACF Documentation](https://www.advancedcustomfields.com/resources/)
- [Gutenberg Handbook](https://developer.wordpress.org/block-editor/)
- [WordPress Coding Standards](https://developer.wordpress.org/coding-standards/)

---

## 🙋 Вопросы?

- Создайте issue с вопросом
- Напишите в обсуждения GitHub
- Присоединяйтесь к нашему чату

---

**Спасибо за ваш вклад! 🎉**
