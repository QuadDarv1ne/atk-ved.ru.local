# Как внести вклад в проект

Спасибо за интерес к проекту АТК ВЭД! Мы рады любому вкладу.

## 🚀 Быстрый старт

1. Fork репозитория
2. Создайте ветку: `git checkout -b feature/amazing-feature`
3. Внесите изменения
4. Commit: `git commit -m 'Add amazing feature'`
5. Push: `git push origin feature/amazing-feature`
6. Откройте Pull Request

## 📋 Процесс разработки

### 1. Настройка окружения

```bash
# Клонировать репозиторий
git clone https://github.com/QuadDarv1ne/atk-ved.ru.local.git
cd atk-ved.ru.local

# Установить зависимости
composer install
npm install

# Скопировать .env
cp .env.example .env
```

### 2. Создание ветки

Используйте префиксы:
- `feature/` - новая функциональность
- `fix/` - исправление бага
- `docs/` - изменения в документации
- `refactor/` - рефакторинг кода
- `test/` - добавление тестов

Примеры:
```bash
git checkout -b feature/add-calculator
git checkout -b fix/contact-form-validation
git checkout -b docs/update-readme
```

### 3. Разработка

Следуйте стандартам кодирования (см. ниже).

### 4. Тестирование

```bash
# PHP тесты
composer test

# Проверка стиля кода
composer phpcs

# Статический анализ
composer phpstan

# JavaScript тесты
npm test

# Линтинг
npm run lint
```

### 5. Commit

Используйте понятные сообщения:

```bash
# Хорошо
git commit -m "Add calculator widget to homepage"
git commit -m "Fix contact form validation error"

# Плохо
git commit -m "fix"
git commit -m "update"
```

### 6. Pull Request

- Опишите изменения
- Добавьте скриншоты (если UI)
- Укажите связанные issues
- Убедитесь, что CI проходит

## 📝 Стандарты кодирования

### PHP

Следуем **PSR-12** и **WordPress Coding Standards**:

```php
<?php
declare(strict_types=1);

namespace ATKVed\Modules;

class ExampleClass {
    private string $property;
    
    public function exampleMethod(string $param): string {
        return esc_html($param);
    }
}
```

Правила:
- Всегда используйте `declare(strict_types=1)`
- Типизация параметров и возвращаемых значений
- Экранирование вывода: `esc_html()`, `esc_url()`, `esc_attr()`
- Валидация ввода: `sanitize_text_field()`, `wp_kses_post()`
- PHPDoc для всех методов

### JavaScript

Следуем **ESLint** конфигурации:

```javascript
/**
 * Example function
 * @param {string} param - Description
 * @returns {string} Result
 */
function exampleFunction(param) {
    return param.trim();
}

// Используйте const/let вместо var
const value = 'example';

// Стрелочные функции
const add = (a, b) => a + b;
```

### CSS

```css
/* BEM методология */
.block {}
.block__element {}
.block--modifier {}

/* Используйте CSS переменные */
:root {
    --color-primary: #e31e24;
    --spacing-unit: 8px;
}

.component {
    color: var(--color-primary);
    padding: var(--spacing-unit);
}
```

## 🧪 Тестирование

### Unit тесты (PHPUnit)

```php
<?php
namespace ATKVed\Tests;

use PHPUnit\Framework\TestCase;

class ExampleTest extends TestCase {
    public function testExample(): void {
        $this->assertTrue(true);
    }
}
```

### E2E тесты (Playwright)

```javascript
test('contact form submission', async ({ page }) => {
    await page.goto('/');
    await page.fill('#name', 'Test User');
    await page.fill('#email', 'test@example.com');
    await page.click('button[type="submit"]');
    await expect(page.locator('.success')).toBeVisible();
});
```

## 📚 Документация

При добавлении новой функциональности:

1. Обновите соответствующий .md файл в `docs/`
2. Добавьте PHPDoc/JSDoc комментарии
3. Обновите CHANGELOG.md
4. Добавьте примеры использования

## 🐛 Сообщение об ошибках

Используйте GitHub Issues с шаблоном:

```markdown
## Описание
Краткое описание проблемы

## Шаги воспроизведения
1. Перейти на...
2. Нажать на...
3. Увидеть ошибку

## Ожидаемое поведение
Что должно было произойти

## Фактическое поведение
Что произошло на самом деле

## Окружение
- WordPress: 6.9.1
- PHP: 8.4
- Браузер: Chrome 120
- ОС: Windows 11
```

## ✨ Предложение функций

Создайте Issue с меткой `enhancement`:

```markdown
## Описание функции
Что вы хотите добавить

## Зачем это нужно
Какую проблему это решает

## Предлагаемое решение
Как это должно работать

## Альтернативы
Другие варианты решения
```

## 🔍 Code Review

Мы проверяем:
- Соответствие стандартам кодирования
- Наличие тестов
- Документация
- Безопасность (XSS, CSRF, SQL Injection)
- Производительность
- Обратная совместимость

## 📋 Чеклист перед PR

- [ ] Код соответствует стандартам
- [ ] Добавлены тесты
- [ ] Все тесты проходят
- [ ] Документация обновлена
- [ ] CHANGELOG.md обновлен
- [ ] Нет конфликтов с main
- [ ] CI проходит успешно

## 🎯 Приоритетные задачи

Смотрите Issues с метками:
- `good first issue` - для новичков
- `help wanted` - нужна помощь
- `priority: high` - высокий приоритет

## 💬 Связь

- GitHub Issues - для багов и предложений
- GitHub Discussions - для вопросов
- Email: maksimqwe42@mail.ru
- Telegram: @quadd4rv1n7

## 📜 Лицензия

Внося вклад, вы соглашаетесь с лицензией GPL-2.0-or-later.

---

**Спасибо за ваш вклад! 🎉**
