# 🚀 Как запустить проект

## Проблема: MySQL не запущен

Ошибка `mysqli_real_connect(): (HY000/2002): Подключение не установлено` означает, что **MySQL сервер не запущен**.

---

## ✅ Решение 1: Запустить через OSPanel (рекомендуется)

### Шаг 1: Откройте OSPanel

1. Найдите значок **OSPanel** в трее Windows (возле часов, может быть скрыт под `^`)
2. Если значка нет — запустите `M:\OSPanel\OSPanel.exe`

### Шаг 2: Запустите сервер

1. **Левый клик** по значку OSPanel (флажок)
2. В меню выберите: **Запустить** → **Перезапустить**
3. Или: **Дополнительно** → **Перезапустить MySQL**

### Шаг 3: Дождитесь запуска

- Когда сервер запустится, флажок станет **зелёным**
- В трее появится сообщение "Сервер запущен"

### Шаг 4: Проверьте MySQL

Откройте браузер: **http://localhost/phpmyadmin**

Если phpMyAdmin открылся — MySQL запущен ✅

### Шаг 5: Откройте сайт

**http://atk-ved.ru.local**

---

## ✅ Решение 2: Использовать скрипт запуска

Запустите файл:

```
start-mysql.bat
```

Или дважды кликните по файлу в проводнике.

---

## ✅ Решение 3: Использовать Docker (альтернатива OSPanel)

Если не хотите использовать OSPanel:

### 1. Остановите OSPanel
- В трее: **Остановить**

### 2. Запустите Docker

```bash
docker-compose up -d
```

### 3. Откройте сайт

**http://localhost:8080**

---

## 🔍 Диагностика

### Проверка порта MySQL

```cmd
netstat -an | findstr ":3306"
```

Если порт 3306 не отображается — MySQL не запущен.

### Проверка через test-db.php

```cmd
php test-db.php
```

Скрипт покажет детальную информацию о подключении.

---

## 📋 Создание базы данных

Если MySQL запущен, но сайт не работает:

### 1. Откройте phpMyAdmin

http://localhost/phpmyadmin

### 2. Создайте базу данных

1. Нажмите вкладку **"Базы данных"**
2. В поле "Имя базы данных": `atk_ved`
3. Кодировка: `utf8mb4_unicode_ci`
4. Нажмите **"Создать"**

### 3. Обновите сайт

http://atk-ved.ru.local

---

## ⚠️ Частые проблемы

### "Port 3306 already in use"

Другой MySQL занимает порт 3306.

**Решение:**
1. Закройте другие MySQL (XAMPP, Denwer, standalone)
2. Перезапустите OSPanel

### "Access denied for user 'root'"

Неверный пароль.

**Решение:**
1. Проверьте `.env` файл
2. Для OpenServer: `WP_DB_PASSWORD=` (пустой)

### MySQL не запускается

**Решение:**
1. Откройте `M:\OSPanel\logs\mysql\`
2. Проверьте логи на ошибки
3. Перезапустите OSPanel с правами администратора

---

## 📞 Поддержка

Если проблема не решена:

1. Проверьте логи: `M:\OSPanel\logs\`
2. Запустите `php test-db.php` для диагностики
3. Убедитесь, что порт 3306 не занят другой программой

---

**После запуска MySQL обновите страницу сайта.** ✅
